apiVersion: v1
kind: Service
metadata:
  name: ai-engine
  namespace: aiops
  labels:
    app: ai-engine
spec:
  type: ClusterIP
  selector:
    app: ai-engine
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
